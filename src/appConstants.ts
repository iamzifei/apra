export const API_URL = "https://jsonplaceholder.typicode.com/photos";

export const PAGE_LIMIT = 25;

export const TOTAL_IMAGES = 5000; // this info is coming from the mock API, usually we can get the total number as part of the API call
